# Build artefacts
preload_data.h
libbionilux_preload.so
*.o
*.bak

# Binaries — never commit compiled output
/bionilux
/box64

# Legacy lib path (pre-v0.2.0) — should not be tracked
lib_x86_64/
lib_x86_64.tar.gz
lib_x86_64*
ovgl
